import { AppColor } from '@src/constants/theme';
export { default as HeaderLeft } from './HeaderLeft';

export const headerStyle = {
  backgroundColor: AppColor.MAIN,
};

export const headerTintColor = AppColor.PRIMARY;
